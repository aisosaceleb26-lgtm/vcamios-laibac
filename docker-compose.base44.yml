services:
  web:
    image: node:22
    working_dir: /app
    command: sh -c "npm install --omit=dev && node virtual-camera-server.js"
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
